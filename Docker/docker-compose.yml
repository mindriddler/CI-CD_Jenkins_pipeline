version: '3.11'
services:
  jenkins:
    image: jenkins/jenkins:jdk17
    privileged: true
    user: root
    ports:      
      - 8080:8080
      - 50000:50000
    volumes:
      - ../jenkins-data:/var/jenkins_home

  backend:
    build:
      context: ..
      dockerfile: Docker/Dockerfile
    ports:
      - 5000:5000
volumes:
  jenkins-data: